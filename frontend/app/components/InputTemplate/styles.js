import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { marginVertical: 30 },
  label: { color: 'grey' },
  input: { height: 40, borderBottomWidth: 1, borderBottomColor: 'grey', minWidth: 300 }
});

export default styles;
