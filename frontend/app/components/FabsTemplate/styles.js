import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  fabsPencil: {
    position: 'absolute',
    margin: 25,
    bottom: 0,
    borderRadius:50,
    right: 0,
     fontSize: 50
    },
    fabsPlus: {
    position: 'absolute',
    margin: 25,
    bottom: 0,
    borderRadius:50,
    right: 0,
    fontSize: 45,
    }
});

export default styles;
