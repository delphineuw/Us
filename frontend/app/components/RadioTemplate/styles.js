import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  label: { color: 'grey', marginBottom: 10 },
  radioContainer: { flexDirection: 'row' },
  radioBox: { marginRight: 40, alignItems: 'center', width: 50 },
  radioButton: { width: 25, height: 25, borderRadius: 12.5, borderWidth: 1, marginBottom: 5 }
});

export default styles;
